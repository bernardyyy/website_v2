---
// Assuming the rest of the file is unchanged above this line
const projects = [...] // Existing projects array

const publishedProjects = projects.filter(project => !project.draft);

document.querySelector('#project-list').innerHTML = publishedProjects.map(project => {
    return `<div>${project.name}</div>`;
}).join('');
---